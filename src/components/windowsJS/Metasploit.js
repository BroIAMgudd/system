import React, { Component } from 'react';

class Metasploit extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  componentDidMount() {
    // const { socket } = this.props;
    // const setState = this.setState.bind(this);
    // Handler(socket, this.print);
  }

  render() {
    // const { vars } = this.state;

    return (
      <div>Hello</div>
    );
  }
}

export default Metasploit;